# Python 学习项目 AI 协作规范

## 1. 项目背景

这是一个 Python 学习项目。

项目中的 `.py` 文件不仅包含可运行的 Python 代码，也包含大量以 `#` 开头的学习笔记、知识总结和代码说明。

我的编程基础：

- 有 C 语言基础
- 有算法训练经历
- 理解基本的数据结构和算法
- 目前正在学习 Python
- 后续计划学习 Python 项目和 Agent 开发

因此，修改或续写笔记时，不需要把我当作完全没有编程基础的初学者。

主要目标是帮助我建立准确、清晰、能够实际使用的 Python 知识，同时特别注意 Python 与 C 语言之间的差异。

---

## 2. AI 的主要任务

你的主要任务是维护、完善和续写 Python 学习笔记。

包括：

1. 检查 Python 语法是否正确
2. 检查 Python 知识和概念是否准确
3. 检查代码示例是否正确
4. 检查注释中的知识描述
5. 修正明显的知识错误
6. 修正明显的错别字
7. 优化不准确或容易产生误解的表述
8. 在必要时补充重要但遗漏的知识
9. 保持笔记适合学习和复习
10. 尽量保持原有笔记结构、学习思路和代码风格
11. 在新增笔记时，尽量模仿已有笔记的结构、表达方式和学习节奏
12. 通过后续知识适当复用和巩固前面已经学习过的 Python 知识

核心目标：

> 帮助我逐步形成能够看懂、编写和维护 Python 项目的实际能力，并为后续 Agent 开发打好 Python 基础。

---

## 3. 笔记修改原则

### 3.1 准确优先

如果笔记存在明确的 Python 知识错误，必须修改。

如果只是表达方式不同，而原来的知识本身是正确的，不要为了追求所谓的专业表达而强行修改。

---

### 3.2 准确不等于复杂

我的目标是学习和掌握 Python，而不是编写 Python 语言规范。

因此：

- 优先使用简单、准确、容易理解的表达
- 不要为了严谨而加入大量底层实现细节
- 不要把高级知识强行加入基础笔记
- 如果一个知识点存在更高级的细节，但当前阶段没有必要了解，可以保持简单表述
- 不要为了追求“绝对严谨”而破坏笔记的可读性

原则：

> 知识正确 > 表述清晰 > 语言专业 > 细节全面

---

### 3.3 保留学习痕迹

不要随意删除原有学习内容。

如果原来的笔记虽然不够严谨，但基本理解是正确的，应优先进行小幅修改，而不是完全重写。

不要因为发现更好的表达方式，就大规模重构原有笔记。

---

### 3.4 保持原有代码风格

尽量保持我的：

- 变量命名方式
- 代码组织方式
- 注释风格
- 示例结构
- 学习顺序

除非存在错误或明显影响理解的问题，否则不要为了追求“最佳实践”而重构代码。

例如，不要仅仅因为代码可以封装成函数、增加类型注解或采用更加工程化的写法，就主动重构基础学习示例。

---

### 3.5 不要过度补充知识

补充知识时，以当前笔记主题为边界。

只补充理解当前知识点所必需的重要内容。

不要因为发现某个知识点还有高级用法，就主动大规模扩展笔记。

如果某个知识属于后续学习阶段，可以保持简洁，不提前展开。

---

### 3.6 默认执行检查并维护

当我要求“检查”“检查笔记”“检查这些内容”等类似任务时，默认理解为：

> 检查 → 判断问题 → 直接修改 → 验证 → 创建维护日志 → 汇总报告

除非我明确说：

- “只检查”
- “只报告问题”
- “不要修改”

或其他具有相同含义的要求。

如果我没有明确禁止修改，则不得仅因为任务使用了“检查”一词，就只进行检查而不执行维护。

---

## 4. 后续 Python 学习笔记续写规范

当我要求新增或续写 Python 学习笔记时，应尽量模仿当前项目已有笔记的结构、表达方式、代码风格和学习节奏。

新增笔记不是为了生成一套独立的标准 Python 教科书，而是为了在现有学习基础上逐步扩展 Python 能力。

---

### 4.1 后续学习目标

后续 Python 学习整体目标是逐步形成：

```text
Python 基础语法
→ 数据结构与程序控制
→ 函数与代码复用
→ 文件、异常、JSON
→ 模块、包与第三方库
→ 面向对象
→ Python 项目
→ Agent 开发
```

但不要把这理解为必须先完整学完所有 Python 语法，才能开始项目。

学习过程中应逐渐从：

```text
语法理解
→ 小案例
→ 多知识点综合
→ 实际 Python 代码
→ Python 项目
```

进行过渡。

---

### 4.2 保持现有笔记风格

当前笔记主要采用：

```text
概念说明
→ 基本语法或基本结构
→ 语法细节
→ 案例
→ 逐渐增加案例难度
→ 综合使用
```

新增笔记应优先延续这种方式。

不要突然改成：

- 大量理论介绍
- 大量专业术语
- API 文档式说明
- 与当前阶段无关的高级知识
- 过度工程化的示例

---

### 4.3 通过案例学习新知识

新增知识应尽量通过简单、直观的案例进行学习。

随着知识增加，可以逐渐让多个已经学习过的知识点组合使用。

例如学习函数时，可以适当结合已经学习过的：

- 变量
- 数据类型
- 字符串
- 列表
- 条件判断
- 循环

例如：

```python
def calculate_total(numbers):
    total = 0

    for number in numbers:
        total += number

    return total
```

这个案例除了学习函数，也可以同时巩固：

- 变量
- 列表
- `for`
- `in`
- 运算符
- `return`

但不要为了“复用旧知识”而强行增加不必要的复杂度。

---

### 4.4 新知识应建立在已有知识上

新增笔记应尽可能使用已经学习过的知识作为基础。

例如：

```text
列表
→ 循环
→ 函数
```

可以逐渐形成：

```text
列表
→ 使用循环处理列表
→ 使用函数封装处理列表的逻辑
```

又例如：

```text
字典
→ JSON
→ API 数据
```

可以逐渐形成：

```text
字典
→ 嵌套字典和列表
→ JSON 数据
→ API 请求与响应中的数据
```

这样做的目的，是通过学习新知识反复巩固旧知识，而不是把每个知识点完全孤立地学习。

---

### 4.5 必要的前置知识可以在上下文中补充

如果新增知识需要使用尚未正式学习的 Python 语法，不要因为严格遵守章节顺序而完全回避。

可以在当前案例中进行必要的、简短的前置说明。

但不要因此提前完整讲解后续章节。

例如：

如果循环案例需要使用列表，可以简单说明列表是什么，以及这里只使用它作为可迭代对象的示例，具体内容留到数据容器章节学习。

---

### 4.6 第一次介绍与后续深入

一个知识点不要求第一次出现时就讲完所有内容。

可以采用：

```text
第一次出现
→ 建立基本认识
→ 掌握最常用语法
→ 配合简单案例

后续章节
→ 在实际使用中再次出现
→ 逐步补充更深入的知识
```

不要为了避免重复而强行把一个知识点一次讲完。

适当重复是允许的，并且可以用于巩固知识。

---

### 4.7 案例难度逐步增加

案例应尽量遵循由简单到复杂的过程：

```text
基本语法
→ 简单案例
→ 多知识点组合
→ 综合案例
→ 更接近实际使用
```

不要在刚介绍一个知识点时就直接使用复杂项目结构。

---

### 4.8 保持学习者视角

我有 C 语言基础和算法训练经历，因此不需要把基础编程概念解释得过于初级。

但涉及 Python 与 C 语言存在明显差异的地方，应主动说明差异。

重点帮助我理解：

- Python 为什么这样写
- Python 与 C 有什么不同
- 这种写法在 Python 中意味着什么
- 这种写法在实际 Python 项目中为什么常见

---

### 4.9 不以“学完所有语法”为前提

不要把 Python 学习理解成：

```text
先学完所有语法
→ 再开始项目
```

更推荐：

```text
掌握当前需要的知识
→ 通过案例使用
→ 学习新的知识
→ 继续综合使用
→ 逐渐接近真实项目
```

如果后续项目需要某个尚未系统学习的知识，可以先进行必要的基础介绍，再在后续章节中系统学习。

---

### 4.10 为 Python 项目和 Agent 开发建立基础

在不提前讲解 Agent 专属内容的情况下，后续 Python 笔记应逐渐帮助我掌握项目开发中常见的 Python 能力。

重点逐步建立：

- 字符串处理
- 列表和字典
- 嵌套数据结构
- 函数
- JSON
- 异常处理
- 文件操作
- 模块和包
- 第三方库
- 面向对象
- 同步与异步基础
- API 数据处理

这些知识应在各自合适的章节中学习。

不要为了“以后学习 Agent”而提前把 Agent 专属知识塞进基础 Python 笔记。

---

### 4.11 不为了分类而强行拆分知识

目录可以按照主题分类，但不要把知识理解成完全互相独立的章节。

例如：

```text
列表
↕
循环
↕
条件判断
↕
函数
```

以及：

```text
字符串
↕
字典
↕
JSON
↕
API 数据
```

这些知识在实际 Python 编程中会经常一起出现。

因此：

> 目录负责保存和查找知识，案例和项目负责建立知识之间的联系。

---

## 5. 问题分类

发现问题时，将问题分为以下三个等级。

### 5.1 🔴 必须修改

包括：

- Python 知识错误
- Python 语法错误
- 会导致代码无法运行的问题
- 会严重误导学习的问题
- 明显错误概念
- 与 Python 实际行为明显不符的描述

---

### 5.2 🟡 建议修改

包括：

- 原来的说法基本正确，但不够准确
- 容易让学习者产生误解
- 表述可以更加清晰
- 示例不够合理
- 术语可以更加规范

如果修改不会明显改善理解，可以保留原文。

---

### 5.3 🔵 可选优化

包括：

- 排版优化
- 注释格式优化
- 普通语言润色
- 不影响知识正确性的表达优化

可选优化不是主要任务。

除非确实有必要，否则不要大量进行可选优化。

---

## 6. Python 准确性检查重点

检查 Python 笔记时，重点关注以下内容：

- Python 中的变量、对象和引用
- 数据类型
- `bool`、`int`、`float` 等类型之间的关系
- 字符串
- 列表
- 元组
- 字典
- 集合
- 切片
- `for`
- `while`
- `range`
- `if`
- `elif`
- `else`
- `match`
- `case`
- `break`
- `continue`
- 循环 `else`
- `and`
- `or`
- `not`
- 运算符
- 函数
- 参数和返回值
- 异常
- 模块和包
- Python 常用内置函数

对于容易受到 C 语言思维影响的知识点，要特别检查 Python 与 C 语言之间的区别。

---

## 7. Python 笔记维护范围

维护范围必须明确。

### 7.1 当前默认维护范围

当前 Python 学习笔记维护范围仅包括：

- `01-/`
- `02-核心语法/`
- `03-数据存储容器/`

这些目录中的 `.py` 文件默认属于 Python 学习笔记维护范围。

---

### 7.2 默认维护内容

在上述维护范围内，检查：

- `.py` 文件中的 Python 学习笔记
- 以 `#` 开头的学习笔记
- 学习示例代码
- 与当前学习内容相关的 Python 代码
- 代码与注释之间是否存在明显矛盾

---

### 7.3 默认不维护内容

以下内容默认不属于 Python 学习笔记维护范围：

- `AGENTS.md`
- `README.md`
- `docs/` 中的文档和日志
- `.git/`
- `.vscode/`
- 编辑器配置文件
- 临时文件
- `tempCodeRunnerFile.py`
- 其他不属于 Python 学习笔记的项目代码

除非我明确要求，否则不要修改上述内容。

---

### 7.4 未来新增目录

以后如果项目中新增学习目录，例如：

- `04-函数/`
- `05-面向对象/`
- `06-文件与异常/`
- `07-模块与包/`
- `08-Agent/`

这些目录默认不自动加入 Python 学习笔记维护范围。

只有当我明确要求将某个目录或文件加入维护范围时，才可以对其进行自动维护。

不要因为新目录中存在 `.py` 文件，就自动检查或修改其中的内容。

---

### 7.5 维护范围优先级

维护范围按照以下优先级确定：

1. 我在当前任务中明确指定的文件
2. 我在当前任务中明确指定的目录
3. 本 `AGENTS.md` 中定义的默认维护范围

例如：

如果我说：

> 检查 `02-核心语法/`

那么本次只检查：

`02-核心语法/`

如果我说：

> 检查 `02-核心语法/04-运算符.py`

那么本次只检查：

`02-核心语法/04-运算符.py`

如果我没有明确指定范围，则使用本文档定义的默认维护范围。

---

### 7.6 不得扩大维护范围

不要因为发现维护范围之外存在问题，就自动扩大维护范围。

例如：

当前任务范围为：

`02-核心语法/`

即使发现：

`03-数据存储容器/`

中的文件存在问题，也不要自动修改。

如果范围之外的问题与当前任务明显相关，可以在最终报告中提醒，但不得修改。

---

### 7.7 可以读取，但不得修改范围外文件

为了理解当前笔记内容，可以在必要时读取维护范围之外的相关文件。

但是：

> 可以读取范围之外的文件，不代表可以修改范围之外的文件。

一次维护任务中，除维护日志外，只允许修改属于本次维护范围的文件。

---

### 7.8 不得自动修改 AGENTS.md

`AGENTS.md` 是项目协作规则文件，不属于 Python 学习笔记。

除非我明确要求修改 `AGENTS.md`，否则任何 Python 笔记维护任务都不得修改它。

不要因为发现 `AGENTS.md` 存在重复、表述问题或格式问题，就主动修改它。

---

## 8. 修改前检查

开始修改前：

1. 确认本次维护范围
2. 确认实际需要检查的文件
3. 读取文件当前内容
4. 根据当前实际文件内容进行判断
5. 确保能够准确记录实际修改前的内容

不得根据之前的检查结果、记忆、推测或假设生成“修改前内容”。

修改日志中的“修改前”必须来自实际文件内容。

---

## 9. Python 代码验证

如果修改了 Python 代码，在条件允许的情况下进行语法检查。

优先检查实际修改过的 Python 文件。

如果代码属于可以安全执行的简单示例，可以运行验证。

如果代码依赖：

- 用户输入
- 外部文件
- 网络
- 第三方库
- 外部服务
- 系统环境
- 其他可能产生副作用的操作

不要为了验证而擅自执行可能产生副作用的操作。

如果无法运行，应进行静态检查，并在最终报告中说明。

---

### 9.1 注释中的示例代码

本项目中的 `.py` 文件大量使用 `#` 将学习案例代码暂时注释起来。

这些被注释的代码虽然当前不会被 Python 执行，但仍然属于 Python 学习笔记的检查范围。

检查时，不要因为代码当前处于注释状态，就默认认为它是正确的。

对于具有明显 Python 代码结构的连续注释内容，应将其视为“学习示例代码”进行检查。

检查时重点判断：

- Python 语法是否正确
- Python 语义和知识是否正确
- 示例代码在取消注释后是否能够正常工作
- 注释内容与示例代码是否一致

如果条件允许，可以将独立的注释代码块提取出来进行静态语法检查。

不要为了验证注释中的案例而直接修改原文件的注释状态。

如果示例包含 `input()`、网络请求、文件操作或其他需要外部条件的操作，不要求直接运行，只需要进行静态检查，并在最终报告中说明。

---

## 10. 修改日志

所有对 Python 学习笔记产生的实际修改，都必须创建维护日志。

日志统一存放在：

`docs/maintenance-log/`

如果目录不存在，则创建。

---

### 10.1 每次维护任务创建一个日志

每一次独立的维护任务只创建一个 Markdown 日志文件。

不要每修改一个地方就创建一个日志文件。

一次维护任务中产生的所有修改，统一记录在同一个日志文件中。

---

### 10.2 日志文件命名

日志文件名使用：

`YYYY-MM-DD_HHMM.md`

例如：

`2026-09-04_0133.md`

时间必须使用实际维护发生的时间，不得虚构。

---

### 10.3 没有修改则不创建日志

如果本次检查没有产生任何实际修改：

- 不创建新的维护日志
- 不为了生成日志而修改文件
- 最终报告中说明“未发现需要修改的内容”

只有实际修改了 Python 学习笔记，才创建维护日志。

---

### 10.4 日志展示方式

维护日志不要使用横向 Markdown 表格记录具体修改。

每一个独立的问题或修改，使用一个独立的小节。

每个问题按照以下顺序记录：

等级：

文件：

位置：

问题类型：

修改原因：

修改前：

修改后：

不同问题之间使用 `---` 分隔。

字段之间保留适当的空行，以提高 Markdown 日志的可读性。

如果“修改前”或“修改后”是代码，应使用 Markdown 代码块。

---

### 10.5 问题等级

使用以下标识：

- 🔴 必须修改
- 🟡 建议修改
- 🔵 可选优化

---

### 10.6 日志基本结构

每个维护日志使用以下结构：

# Python 学习笔记维护日志

## 一、基本信息

维护时间：

维护范围：

检查文件数量：

修改文件数量：

实际修改处数：

---

## 二、修改统计

🔴 必须修改：

🟡 建议修改：

🔵 可选优化：

合计：

---

## 三、修改记录

每一个独立的实际修改使用一个独立的小节。

示例：

### 1. 🟡 建议修改

等级：

🟡 建议修改

文件：

`02-核心语法/05-核心流程控制语句/02-结构模式匹配.py`

位置：

第 4 行附近。

问题类型：

表述不准确。

修改原因：

原来的描述容易让人产生误解，因此需要补充更加准确的说明。

修改前：

```python
case "pattern1":
```

修改后：

```python
case pattern1:
```

---

### 2. 🔴 必须修改

等级：

🔴 必须修改

文件：

`02-核心语法/05-核心流程控制语句/03-循环语句.py`

位置：

第 31 行附近。

问题类型：

知识错误。

修改原因：

原来的描述与 Python 实际行为不一致。

修改前：

```text
原来的错误描述
```

修改后：

```text
修改后的正确描述
```

---

## 四、代码验证

记录本次实际进行的验证。

例如：

Python 语法检查：

✅ 通过

修改后的简单示例运行：

✅ 已验证

需要用户输入的示例：

⚪ 未运行，仅进行静态检查

需要外部依赖的示例：

⚪ 未运行，仅进行静态检查

如果某项没有执行，应说明原因。

---

## 五、范围外问题

如果发现维护范围之外存在潜在问题，只进行提醒，不修改。

例如：

⚠️ 维护范围之外发现潜在问题：

`04-函数/xxx.py` 中可能存在一个函数参数相关的问题。

本次未修改，因为 `04-函数/` 不属于当前维护范围。

如果没有发现范围外问题：

✅ 未发现维护范围之外的潜在问题。

---

## 六、维护总结

检查文件：

修改文件：

实际修改：

🔴 必须修改：

🟡 建议修改：

🔵 可选优化：

Python 语法检查：

维护日志：

范围外问题：

---

## 11. 修改完成后的报告

维护完成后，向我报告以下内容：

1. 本次维护范围
2. 检查了多少个文件
3. 修改了多少个文件
4. 实际修改了多少处
5. 必须修改多少处
6. 建议修改多少处
7. 可选优化多少处
8. 是否创建维护日志
9. 日志文件路径
10. 是否进行了 Python 语法检查
11. 是否运行了可安全运行的代码示例
12. 是否发现维护范围之外的潜在问题

如果没有发现需要修改的内容，应明确告诉我：

> 未发现需要修改的内容。

如果发现范围之外的问题，只进行提醒，不修改。

---

## 12. 默认工作流程

除非我明确要求其他方式，否则按照以下流程执行：

1. 确认维护范围
2. 找出属于维护范围的文件
3. 读取并检查当前文件内容
4. 判断问题等级
5. 直接修改需要修改的 Python 学习笔记
6. 对修改后的 Python 代码进行适当的语法检查或安全验证
7. 根据实际修改生成本次维护日志
8. 汇总维护结果
9. 报告范围之外发现但未修改的问题

如果是新增或续写 Python 学习笔记，则按照以下流程：

1. 确认新增笔记的主题和位置
2. 阅读相关已有笔记，了解当前学习基础和写作风格
3. 判断新增知识需要哪些前置知识
4. 保持已有笔记的表达方式和案例节奏
5. 编写概念说明和基本语法
6. 通过案例逐步增加难度
7. 适当复用已经学习过的 Python 知识
8. 对新增内容进行 Python 语法和知识检查
9. 检查是否出现不必要的高级知识或知识前置
10. 完成后向我汇总新增内容和学习衔接关系

如果我明确要求“只分析”“只规划”“不要写入文件”，则不得创建或修改文件。

---

## 13. 最重要的规则

始终遵守以下原则：

> 只维护明确属于维护范围的 Python 学习笔记。

当前默认维护范围：

- `01-/`
- `02-核心语法/`
- `03-数据存储容器/`

未来新增目录默认不加入维护范围。

除非我明确要求，否则：

- 不要扩大维护范围
- 不要修改范围之外的文件
- 不要修改 `AGENTS.md`
- 不要为了追求“最佳实践”而重构正确的学习代码
- 不要为了追求绝对严谨而让基础笔记变得复杂
- 不要虚构修改日志
- 没有实际修改时不要创建日志
- 不要把后续笔记写成脱离当前学习基础的标准教材
- 不要为了分类而强行拆分实际相互关联的知识
- 不要为了以后学习 Agent 而提前加入 Agent 专属知识
- 不要把“学完所有 Python 语法”作为进入 Python 项目的前提

核心目标：

> **帮助我建立准确、清晰、适合长期复习的 Python 知识体系，同时尽可能保留我的学习痕迹和原有思路，并逐步形成能够阅读、编写和维护 Python 项目的能力。**