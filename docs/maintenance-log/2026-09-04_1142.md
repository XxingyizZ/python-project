# Python 学习笔记维护日志

## 一、基本信息

维护时间：

2026-09-04 11:42

维护范围：

`21-项目06-MCP基础/`

检查文件数量：

5 个 Python 文件，以及项目说明、依赖、环境变量示例和示例笔记文件。

修改文件数量：

12 个文件（创建 11 个项目文件，创建 1 个维护日志）。

实际修改处数：

1 个完整 MCP 基础项目。

---

## 二、修改统计

🔴 必须修改：

0

🟡 建议修改：

0

🔵 可选优化：

0

合计：

1 个新增项目任务。

---

## 三、修改记录

### 1. 新增 MCP 基础实战项目

等级：

🔵 可选优化

文件：

`21-项目06-MCP基础/`

位置：

新建项目目录。

问题类型：

新增章节项目。

修改原因：

建立一个不依赖大模型的最小 MCP Client/Server 示例，帮助学习者理解 Host、Client、Server、Tool 和 stdio 通信的基本关系。

修改前：

```text
项目目录不存在。
```

修改后：

```text
创建 README.md、client.py、server.py、tools.py、config.py、main.py、requirements.txt、.env.example，以及 data/notes/ 下的三个示例笔记文件。
```

项目内容包括：

- `calculate` 安全计算工具；
- 白名单笔记列表和读取工具；
- MCP Server 工具注册；
- MCP Client 的工具发现与调用；
- stdio 子进程通信；
- 配置集中管理和安全边界说明。

---

## 四、代码验证

Python 语法检查：

✅ 通过。已检查 `config.py`、`tools.py`、`server.py`、`client.py` 和 `main.py`。

注释及示例代码静态检查：

✅ 已检查。项目中的示例代码与实际 Python 文件结构一致。

安全本地工具验证：

✅ 已验证计算、笔记列表、笔记读取、除数为 0 拒绝和路径穿越拒绝。

MCP Client/Server 协议联调：

⚪ 未运行。本机未安装 `mcp` 包，未安装依赖，也未伪造协议调用成功结果。

真实网络或外部服务调用：

⚪ 未执行。本项目不需要网络和 API Key。

---

## 五、范围外问题

✅ 未发现因本次任务需要修改的维护范围之外问题。

本次未修改前面章节、其他项目目录或 `AGENTS.md`。

---

## 六、维护总结

检查文件：

`21-项目06-MCP基础/` 中的 5 个 Python 文件及相关项目文件。

修改文件：

新建 `21-项目06-MCP基础/` 项目文件，并创建本日志。

实际修改：

完成 MCP 基础 Client/Server 项目。

🔴 必须修改：0

🟡 建议修改：0

🔵 可选优化：0

Python 语法检查：

✅ 通过。

维护日志：

`docs/maintenance-log/2026-09-04_1142.md`

范围外问题：

✅ 未发现；前面章节和 `AGENTS.md` 均未修改。
