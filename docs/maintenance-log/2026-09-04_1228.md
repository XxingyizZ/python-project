# Python 学习笔记维护日志

## 一、基本信息

维护时间：

2026-09-04 12:28

维护范围：

课程知识衔接与项目 README 教学说明，涉及 04、14、15 章节以及 18–23 项目 README。

检查文件数量：

检查 01–23 的目录和关键课程/项目文件；实际重点修改 10 个文件。

修改文件数量：

10 个文件。

实际修改处数：

10 处桥接补充。

---

## 二、修改统计

🔴 必须修改：

0

🟡 建议修改：

10

🔵 可选优化：

0

合计：

10

---

## 三、修改记录

### 1. 🟡 建议修改：补充函数到 Tool 的桥接

等级：

🟡 建议修改

文件：

`04-函数/01-函数基础.py`

位置：

函数基础笔记末尾。

问题类型：

前置知识缺少后续应用桥接。

修改原因：

后续 Agent 会把 Python 函数作为工具能力管理。补充函数赋值给变量和放入字典的最小示例，帮助连接函数、工具注册和白名单。

修改前：

```text
笔记只介绍函数定义、调用和 Python 与 C 的函数语法差异。
```

修改后：

```python
func = hello
func()

tools = {"hello": hello}
tools["hello"]()
```

---

### 2. 🟡 建议修改：补充 Context 和 Token 基础认识

等级：

🟡 建议修改

文件：

`14-LLM应用开发/03-消息与Prompt.py`

位置：

消息与 Prompt 基础说明之后。

问题类型：

LLM 输入、历史、工具结果和后续 RAG/Memory 之间缺少统一概念。

修改原因：

明确 Context 可以由用户问题、instructions、历史消息、Memory 和工具结果组成，并解释 Token 与上下文范围的关系，为后续限制历史、Top-K 和工具结果作准备。

修改前：

```text
笔记介绍 Prompt、input、instructions 和消息列表，但没有统一说明 Context 和 Token。
```

修改后：

```text
增加 Context 的定义、Token 的基础概念，以及历史消息、Top-K 和工具结果需要受限的原因。
```

---

### 3. 🟡 建议修改：补充 Agent State

等级：

🟡 建议修改

文件：

`15-Agent开发/04-Agent循环.py`

位置：

Agent 循环基础说明末尾。

问题类型：

Agent Loop 中已有状态数据未被概念化。

修改原因：

用简单的“Agent = Loop + State”解释 messages/context、tool calls、tool results、Memory、检索结果和 step count 的关系，不引入状态机或框架。

修改前：

```text
笔记解释了工具调用循环和最大步数，但没有提出 Agent State。
```

修改后：

```text
增加 Agent = Loop + State，以及每一步会产生新状态的说明。
```

---

### 4. 🟡 建议修改：补充异常跨层衔接

等级：

🟡 建议修改

文件：

`15-Agent开发/06-Agent异常处理与安全.py`

位置：

基础异常处理和工具白名单说明之后。

问题类型：

Python Exception 到 Agent Failure 的关系只在不同章节中分别出现。

修改原因：

显式补充“Python Exception → API Error → Tool Error → Agent Failure”，帮助理解不同层负责处理自己的错误。

修改前：

```text
笔记分别介绍工具异常、参数验证和最大步数，没有总结跨层错误链路。
```

修改后：

```text
增加 Python Exception → API Error → Tool Error → Agent Failure 的简短总结。
```

---

### 5. 🟡 建议修改：明确 File Tool 到 RAG 的递进

等级：

🟡 建议修改

文件：

`18-项目03-文件工具Agent/README.md`、`19-项目04-RAG基础/README.md`

位置：

Project 18 的 RAG 说明和 Project 19 的 RAG 定义。

问题类型：

项目之间的新增能力关系可以更加直接。

修改原因：

说明 Project 18 沿用 Project 17 的 Agent Loop 并新增受限文件 Tool；Project 19 在此基础上学习文档切分、Embedding 和相似度，同时强调 RAG 不等于余弦相似度。

修改前：

```text
两个项目分别解释文件搜索和 RAG，但项目递进关系不够显式。
```

修改后：

```text
增加 Project 18 → Project 19 的递进说明，并明确 RAG 的本质是检索相关信息并提供上下文。
```

---

### 6. 🟡 建议修改：统一 Conversation、Memory、RAG、Tool 边界

等级：

🟡 建议修改

文件：

`20-项目05-MemoryAgent/README.md`、`23-最终项目-个人学习Agent/README.md`

位置：

Memory 说明和最终项目的 Memory/RAG 说明。

问题类型：

相关概念虽已分别出现，但边界不够集中。

修改原因：

补充 Conversation 表示最近对话、Memory 表示长期状态、RAG 表示外部知识检索、Tool 表示当前动作的简短区分。

修改前：

```text
README分别解释Memory和RAG，但没有在最终项目中用一句话统一四者边界。
```

修改后：

```text
增加统一边界说明，并保留 previous_response_id 不等于 Memory 的原有说明。
```

---

### 7. 🟡 建议修改：明确 MCP Tools、Resources、Prompts 的实现范围

等级：

🟡 建议修改

文件：

`21-项目06-MCP基础/README.md`

位置：

MCP 角色说明。

问题类型：

架构图列出 Tools、Resources、Prompts，但代码实际只实现 Tools。

修改原因：

明确 Resource 和 Prompt 当前只做概念介绍，实际实现范围只有 Tools，避免学习者误以为三者都已编码。

修改前：

```text
README说明Resource暂不实现，但没有明确说明Prompt也只停留在概念层。
```

修改后：

```text
增加 Prompt 只做概念介绍、当前代码只实现 Tools 的说明。
```

---

### 8. 🟡 建议修改：明确 Project 22 的综合定位

等级：

🟡 建议修改

文件：

`22-项目07-Agent+RAG+Memory综合项目/README.md`

位置：

项目目标段落。

问题类型：

与前置项目的关系不够显式。

修改原因：

说明 Project 22 是组合 Project 17、19 和 20 已有机制，而不是重新学习一遍 Agent、RAG 和 Memory。

修改前：

```text
本项目把前面学过的内容组合起来。
```

修改后：

```text
增加“组合 Project 17、19 和 20 已经建立的机制”的说明。
```

---

## 四、代码验证

Python 语法检查：

✅ 通过。已检查实际修改的 4 个 Python 文件。

注释示例静态检查：

✅ 已确认新增注释代码结构正确，并独立验证函数作为对象的示例。

README 结构检查：

✅ 已检查 18–23 项目 README 的递进说明与实际项目内容一致。

安全示例运行：

✅ 仅运行无输入、无网络、无外部服务的函数对象示例；未运行 API、用户输入或文件写入案例。

---

## 五、范围外问题

⚠️ 发现：

`22-项目07-Agent+RAG+Memory综合项目/__pycache__` 是维护范围之外的既有生成缓存目录。本次未修改或删除，因为任务要求保持无关范围不变。

未修改：

`AGENTS.md`、01–03 章节、16–17 项目及项目代码文件。

---

## 六、维护总结

检查文件：

01–23 目录结构及相关核心章节、项目 README。

修改文件：

10 个文件。

实际修改：

补充函数→Tool、LLM→Context、Agent Loop→State、异常跨层、File Tool→RAG、概念边界、MCP 实现范围和项目递进说明。

🔴 必须修改：0

🟡 建议修改：10

🔵 可选优化：0

Python 语法检查：

✅ 通过。

维护日志：

`docs/maintenance-log/2026-09-04_1228.md`

范围外问题：

发现既有 `22-项目07-Agent+RAG+Memory综合项目/__pycache__`，未修改。

